<Window x:Class="FrontLineGUI.Include.Components.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FrontLineGUI.Include.Components"
        mc:Ignorable="d"
        Title="Message" Height="200" Width="400"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterOwner">

    <Border CornerRadius="5" Background="#F5F5F5" BorderBrush="#333" BorderThickness="1" MouseDown="Window_MouseDown">
        
        <!-- Pop-In Animation -->
        <Border.RenderTransform>
            <ScaleTransform x:Name="WindowScale" CenterX="200" CenterY="100" ScaleX="0.8" ScaleY="0.8"/>
        </Border.RenderTransform>

        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="WindowScale" 
                                 Storyboard.TargetProperty="ScaleX" 
                                 To="1.0" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <BackEase Amplitude="0.5" EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WindowScale" 
                                 Storyboard.TargetProperty="ScaleY" 
                                 To="1.0" Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <BackEase Amplitude="0.5" EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtTitle" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            <TextBlock x:Name="txtMessage" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Center"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="btnOk" Content="OK" Margin="0" Click="BtnOk_Click" Style="{ StaticResource GeneralButton }"/>
                <Button x:Name="btnCancel" Content="Cancel" Margin="2,0,0,0" Click="BtnCancel_Click" Style="{ StaticResource GeneralButton }" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
